console.log("Welcome.")

console.log(Math.max(1, 3, 2));

console.log(Math.max(-1, -3, -2));

const array1 = [1, 3, 2];

console.log(Math.max(...array1));


console.log("Now we will find the greatest Number.")

prompt();

function compare(){

const array2 = [Number(process.argv[process.argv.length])];

if ([Number(process.argv[process.argv.length])] >= 2) {
 //return console.log((Math.max(...[Number(process.argv[process.argv.length])])));
return  max([Number(process.argv.length)])
}
}

function prompt(){
 console.log("Here is another function.")

    let arrayExample = [];

    for (let i = 2; i < Number(process.argv.length); i++) {
        arrayExample.push(i);
        console.log(arrayExample);

}


function manyArgs() {
    arguments = Number(process.args[i]);
     for (var i = 0; i < arguments.length; ++i)
      alert(arguments[i])
}
}